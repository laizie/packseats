# Runtime state (last-seen seat counts)
data/*
!data/.gitkeep

# Real config with any tokens — only the example is committed
config/watches.json
.env

# Markdown docs — only README is public; the rest stay local
*.md
!README.md

# Python
.venv/
__pycache__/

# OS noise
.DS_Store

# Language-specific entries go here once the stack is locked in Phase 0
